[package]
name = "wasm-context-provider"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
keystack-wasm-guest = { version = "0.1.0", path = "../../crates/keystack-wasm-guest" }
